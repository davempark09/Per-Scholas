import React from 'react';

function Success (props) {
        return (  
            <div>
                <h1>Welcome {props.username}</h1>
            </div>
        )
    }

export default Success;